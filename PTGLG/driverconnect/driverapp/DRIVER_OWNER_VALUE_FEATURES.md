# 🚛 Driver & Truck Owner Value Features

> **Target Users:** คนขับรถ + เจ้าของรถ  
> **Goal:** เพิ่มประสิทธิภาพ ลดต้นทุน เพิ่มรายได้  
> **Approach:** Data-driven insights + Actionable recommendations

---

## 🎯 Pain Points & Solutions

### **สำหรับคนขับ (Driver)**
| Pain Point | Feature Solution |
|------------|------------------|
| ไม่รู้ว่าทำงานได้ดีแค่ไหน | Performance Dashboard + Driver Score |
| ไม่รู้ว่าจะได้ bonus ไหม | Incentive Calculator + Achievement System |
| ใช้น้ำมันเยอะ แต่ไม่รู้ว่าเยอะกว่าปกติไหม | Fuel Efficiency Tracking + Comparison |
| ทำงาน overtime แต่ไม่มีใครเห็น | Work Hours Tracker + Overtime Alert |
| ไม่รู้ route ไหนประหยัดเวลา | Route Analytics + Best Route Suggestion |
| รถเสียบ่อย ทำงานไม่ได้ | Maintenance Alert + Preventive Reminder |

### **สำหรับเจ้าของรถ (Truck Owner)**
| Pain Point | Feature Solution |
|------------|------------------|
| ไม่รู้ต้นทุนจริงต่อเที่ยว | Cost Tracking + Profit Calculator |
| ไม่รู้ว่ารถคันไหนทำกำไรดี | Vehicle Performance Comparison |
| รถวิ่งไม่เต็มศักยภาพ | Utilization Dashboard + Idle Time Alert |
| ค่าซ่อมสูง | Predictive Maintenance + Cost Analysis |
| คนขับบางคนขับไม่ดี | Driver Performance Ranking |
| ไม่รู้ว่าควรลงทุนซื้อรถเพิ่มไหม | Business Intelligence Dashboard |

---

## 📊 Feature Roadmap

### **QUICK WINS (Week 1-2)** 🔴 เห็นผลเร็ว

#### 1. Fuel Efficiency Tracker
```
Goal: ให้คนขับรู้ว่าใช้น้ำมันประหยัดหรือไม่

Display:
┌─────────────────────────────────────────────┐
│ ⛽ ประสิทธิภาพน้ำมันวันนี้                  │
├─────────────────────────────────────────────┤
│ อัตราสิ้นเปลือง:  4.2 km/L                 │
│ เปรียบเทียบ:      ดีกว่าค่าเฉลี่ย 12%      │
│                    █████████░ 90/100        │
│                                              │
│ 📈 เทรนด์ 7 วันที่ผ่านมา                   │
│ [Chart: Line graph showing daily efficiency]│
│                                              │
│ 💰 ประหยัดได้สัปดาห์นี้: ฿450             │
│ 🏆 Ranking: #3 จาก 45 คน                   │
└─────────────────────────────────────────────┘

Implementation:
- เก็บ start_odo, end_odo, fuel_amount
- คำนวณ km/L อัตโนมัติ
- เปรียบเทียบกับค่าเฉลี่ย fleet
- แสดง ranking + reward ถ้าดีที่สุด
```

#### 2. Trip Cost Calculator
```
Goal: รู้ต้นทุนจริงของแต่ละเที่ยว

Display:
┌─────────────────────────────────────────────┐
│ 💰 ต้นทุนเที่ยวนี้                         │
├─────────────────────────────────────────────┤
│ รายได้:           ฿3,500                    │
│                                              │
│ ค่าใช้จ่าย:                                 │
│ - น้ำมัน:         ฿1,200  (34%)            │
│ - ค่าผ่านทาง:     ฿120    (3%)             │
│ - ค่าอาหาร:       ฿180    (5%)             │
│ - ค่าเสื่อม:      ฿400    (11%)            │
│ ─────────────────────────────              │
│ รวมค่าใช้จ่าย:    ฿1,900                    │
│                                              │
│ กำไรสุทธิ:        ฿1,600  (46%)            │
│ เปรียบเทียบ:      ดีกว่าค่าเฉลี่ย ฿200     │
└─────────────────────────────────────────────┘

Implementation:
- เก็บ fuel cost, toll cost, meal cost
- คำนวณ depreciation อัตโนมัติ
- แสดง profit margin
- เปรียบเทียบกับเที่ยวอื่นๆ
```

#### 3. Driver Performance Score
```
Goal: ให้คนขับรู้ว่าทำงานได้ดีแค่ไหน

Display:
┌─────────────────────────────────────────────┐
│ 🏆 Driver Score: 87/100  (Excellent!)      │
├─────────────────────────────────────────────┤
│ การส่งตรงเวลา:    ████████░░ 92/100        │
│ ประสิทธิภาพน้ำมัน: ███████░░░ 85/100        │
│ การดูแลรถ:        █████████░ 90/100        │
│ ความปลอดภัย:      ████████░░ 88/100        │
│ ความพึงพอใจลูกค้า: ████████░░ 82/100        │
│                                              │
│ 📈 Trend: +5 points จากเดือนที่แล้ว         │
│                                              │
│ 💡 คำแนะนำ:                                │
│ - ปรับปรุงการเบรกให้นุ่มนวล → ประหยัดน้ำมัน│
│ - Check-in ก่อนถึง 5 นาที → ส่งตรงเวลา      │
└─────────────────────────────────────────────┘

Calculation:
- On-time delivery rate (30%)
- Fuel efficiency vs average (25%)
- Vehicle condition reports (20%)
- Safety score (no accidents) (15%)
- Customer ratings (10%)
```

---

### **CORE FEATURES (Week 3-6)** 🟡 สำคัญมาก

#### 4. Daily Performance Dashboard (For Driver)
```javascript
// New page: dashboard.html

class DriverDashboard {
  async loadToday() {
    const today = new Date().toISOString().split('T')[0];
    
    // Today's stats
    const stats = await supabase
      .from('v_driver_daily_stats')
      .select('*')
      .eq('driver_user_id', this.userId)
      .eq('date', today)
      .single();
    
    return {
      trips_completed: stats.trips_completed || 0,
      total_distance_km: stats.total_distance_km || 0,
      total_duration_hours: stats.total_duration_hours || 0,
      fuel_efficiency_kml: stats.fuel_efficiency_kml || 0,
      revenue_today: stats.revenue_today || 0,
      costs_today: stats.costs_today || 0,
      net_profit: stats.net_profit || 0,
      performance_score: stats.performance_score || 0,
      rank_today: stats.rank_today || null
    };
  }
  
  render() {
    // UI showing all key metrics
  }
}
```

**UI Design:**
```
┌─────────────────────────────────────────────────────────────┐
│ 📊 ภาพรวมวันนี้ - 22 ม.ค. 2026                             │
├─────────────────────────────────────────────────────────────┤
│ สถานะ: 🚛 กำลังขนส่ง (เที่ยว 2/3)                          │
│                                                              │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│ │ เที่ยว   │ │ ระยะทาง  │ │ เวลา     │ │ รายได้    │       │
│ │   2      │ │ 287 km   │ │ 6.5 hr   │ │ ฿7,200   │       │
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                                                              │
│ ⛽ ประสิทธิภาพน้ำมัน:  4.2 km/L  (ดีกว่าค่าเฉลี่ย 12%)    │
│ 💰 กำไรสุทธิ:          ฿3,450    (48% margin)              │
│ 🏆 Performance Score:  87/100    (#3 ในทีม)                │
│                                                              │
│ ────────────────────────────────────────                    │
│ สัปดาห์นี้                                                  │
│ เที่ยว:     12    เป้าหมาย: 15  (80%)                      │
│ รายได้:     ฿42,500                                         │
│ กำไร:       ฿19,800  (47% margin)                          │
│                                                              │
│ 🎯 เป้าหมายสัปดาห์: ขาดอีก 3 เที่ยว → Bonus ฿2,000!      │
└─────────────────────────────────────────────────────────────┘
```

---

#### 5. Cost Breakdown System
```sql
-- New table: trip_costs
CREATE TABLE trip_costs (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  trip_id bigint REFERENCES trips(id),
  driver_user_id text,
  vehicle_id bigint,
  
  -- Fuel costs
  fuel_amount_liters numeric,
  fuel_price_per_liter numeric,
  fuel_total_cost numeric,
  
  -- Toll costs
  toll_total numeric,
  toll_receipts jsonb, -- array of toll receipts
  
  -- Meal costs
  meal_total numeric,
  meal_receipts jsonb,
  
  -- Parking costs
  parking_total numeric,
  
  -- Maintenance costs (if any during trip)
  maintenance_total numeric,
  maintenance_notes text,
  
  -- Other costs
  other_costs numeric,
  other_notes text,
  
  -- Revenue
  trip_revenue numeric,
  
  -- Calculated fields
  total_costs numeric GENERATED ALWAYS AS (
    COALESCE(fuel_total_cost, 0) +
    COALESCE(toll_total, 0) +
    COALESCE(meal_total, 0) +
    COALESCE(parking_total, 0) +
    COALESCE(maintenance_total, 0) +
    COALESCE(other_costs, 0)
  ) STORED,
  
  net_profit numeric GENERATED ALWAYS AS (
    COALESCE(trip_revenue, 0) - (
      COALESCE(fuel_total_cost, 0) +
      COALESCE(toll_total, 0) +
      COALESCE(meal_total, 0) +
      COALESCE(parking_total, 0) +
      COALESCE(maintenance_total, 0) +
      COALESCE(other_costs, 0)
    )
  ) STORED,
  
  profit_margin numeric GENERATED ALWAYS AS (
    CASE 
      WHEN COALESCE(trip_revenue, 0) > 0 THEN
        ((COALESCE(trip_revenue, 0) - (
          COALESCE(fuel_total_cost, 0) +
          COALESCE(toll_total, 0) +
          COALESCE(meal_total, 0) +
          COALESCE(parking_total, 0) +
          COALESCE(maintenance_total, 0) +
          COALESCE(other_costs, 0)
        )) / COALESCE(trip_revenue, 0)) * 100
      ELSE 0
    END
  ) STORED,
  
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);

-- Index
CREATE INDEX idx_trip_costs_driver ON trip_costs(driver_user_id);
CREATE INDEX idx_trip_costs_vehicle ON trip_costs(vehicle_id);
CREATE INDEX idx_trip_costs_date ON trip_costs(created_at);
```

**UI: Cost Entry Form (ตอนจบเที่ยว)**
```
┌─────────────────────────────────────────────┐
│ 💰 บันทึกค่าใช้จ่าย - เที่ยว #2601S16472   │
├─────────────────────────────────────────────┤
│ ⛽ น้ำมัน                                   │
│ จำนวน: [____] ลิตร  ราคา: [____] บาท/ลิตร │
│ รวม: ฿1,200  📸 [ถ่ายรูปใบเสร็จ]           │
│                                              │
│ 🛣️ ค่าผ่านทาง                              │
│ จำนวน: [____] บาท  📸 [ถ่ายรูปใบเสร็จ]     │
│                                              │
│ 🍽️ ค่าอาหาร                                │
│ จำนวน: [____] บาท  (Optional)              │
│                                              │
│ 🅿️ ค่าจอดรถ                                │
│ จำนวน: [____] บาท  (Optional)              │
│                                              │
│ 🔧 ค่าซ่อมบำรุง (ถ้ามี)                    │
│ จำนวน: [____] บาท  หมายเหตุ: [____]       │
│                                              │
│ 📦 อื่นๆ                                    │
│ จำนวน: [____] บาท  หมายเหตุ: [____]       │
│                                              │
│ ─────────────────────────────              │
│ รวมค่าใช้จ่าย: ฿1,900                       │
│                                              │
│ [💾 บันทึก] [❌ ยกเลิก]                    │
└─────────────────────────────────────────────┘

Features:
- OCR scan receipts (future)
- Auto-calculate total
- Save photos to Supabase Storage
- Generate expense report
```

---

#### 6. Vehicle Performance Comparison (For Owner)
```
Goal: เจ้าของรถดูว่ารถคันไหนทำกำไรดีที่สุด

Display:
┌─────────────────────────────────────────────────────────────┐
│ 🚛 เปรียบเทียบประสิทธิภาพรถ (เดือน ม.ค. 2026)              │
├─────────────────────────────────────────────────────────────┤
│ Sorting: [กำไรสูงสุด ▾]  Filter: [ทุกประเภท ▾]            │
│                                                              │
│ ┌────────────────────────────────────────────────────┐     │
│ │ 🥇 #1: กข-1234 กรุงเทพ (10-wheel truck)            │     │
│ │ เที่ยว: 28  |  กำไร: ฿48,500  |  Margin: 52%      │     │
│ │ น้ำมัน: 4.5 km/L  |  Uptime: 95%  |  Score: 92/100 │     │
│ │ [ดูรายละเอียด]                                     │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ ┌────────────────────────────────────────────────────┐     │
│ │ 🥈 #2: ขค-5678 กรุงเทพ (6-wheel truck)             │     │
│ │ เที่ยว: 32  |  กำไร: ฿45,200  |  Margin: 48%      │     │
│ │ น้ำมัน: 4.2 km/L  |  Uptime: 92%  |  Score: 88/100 │     │
│ │ [ดูรายละเอียด]                                     │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ ┌────────────────────────────────────────────────────┐     │
│ │ ⚠️ #15: ฮฮ-9999 กรุงเทพ (10-wheel truck)           │     │
│ │ เที่ยว: 18  |  กำไร: ฿18,500  |  Margin: 32%      │     │
│ │ น้ำมัน: 3.1 km/L  |  Uptime: 75%  |  Score: 62/100 │     │
│ │ ⚠️ ใช้น้ำมันสูง, ซ่อมบ่อย  [วิเคราะห์]            │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ 📊 สรุปภาพรวม:                                              │
│ - รถทั้งหมด: 45 คัน                                         │
│ - กำไรเฉลี่ย: ฿35,800/คัน/เดือน                            │
│ - Best performer: +35% กว่าค่าเฉลี่ย                        │
│ - Worst performer: -48% กว่าค่าเฉลี่ย (ควรตรวจสอบ!)        │
└─────────────────────────────────────────────────────────────┘

SQL View:
CREATE VIEW v_vehicle_monthly_performance AS
SELECT 
  v.id as vehicle_id,
  v.registration_no,
  v.vehicle_type,
  DATE_TRUNC('month', t.created_at) as month,
  COUNT(t.id) as total_trips,
  SUM(tc.trip_revenue) as total_revenue,
  SUM(tc.total_costs) as total_costs,
  SUM(tc.net_profit) as total_profit,
  AVG(tc.profit_margin) as avg_profit_margin,
  AVG(f.fuel_efficiency_kml) as avg_fuel_efficiency,
  COUNT(*) FILTER (WHERE m.severity = 'HIGH') as high_maintenance_count,
  (COUNT(DISTINCT DATE(t.created_at))::float / 
   DATE_PART('day', DATE_TRUNC('month', CURRENT_DATE) + INTERVAL '1 month' - INTERVAL '1 day')) * 100 
   as uptime_percent
FROM vehicles v
LEFT JOIN trips t ON t.vehicle_id = v.id
LEFT JOIN trip_costs tc ON tc.trip_id = t.id
LEFT JOIN fuel_records f ON f.trip_id = t.id
LEFT JOIN maintenance_records m ON m.vehicle_id = v.id 
  AND DATE_TRUNC('month', m.created_at) = DATE_TRUNC('month', t.created_at)
GROUP BY v.id, v.registration_no, v.vehicle_type, DATE_TRUNC('month', t.created_at);
```

---

#### 7. Predictive Maintenance Alert
```
Goal: แจ้งเตือนก่อนรถเสีย ลดต้นทุนซ่อม

Features:
1. Mileage-based alerts
2. Time-based alerts  
3. Condition-based alerts
4. Cost prediction

Database:
CREATE TABLE maintenance_schedule (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  vehicle_id bigint REFERENCES vehicles(id),
  maintenance_type text, -- 'OIL_CHANGE', 'TIRE_ROTATION', 'BRAKE_CHECK', etc.
  
  -- Trigger conditions
  trigger_type text, -- 'MILEAGE', 'TIME', 'CONDITION'
  trigger_value numeric, -- e.g., 5000 km or 3 months
  last_done_at timestamptz,
  last_done_odo numeric,
  
  -- Next due
  next_due_odo numeric,
  next_due_date date,
  
  -- Cost
  estimated_cost numeric,
  actual_cost numeric,
  
  -- Status
  status text DEFAULT 'SCHEDULED', -- SCHEDULED, DUE, OVERDUE, COMPLETED
  
  created_at timestamptz DEFAULT now()
);

CREATE TABLE maintenance_records (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  vehicle_id bigint REFERENCES vehicles(id),
  maintenance_type text,
  performed_at timestamptz DEFAULT now(),
  odo_reading numeric,
  cost numeric,
  parts_replaced jsonb,
  notes text,
  receipt_url text,
  performed_by text,
  
  created_at timestamptz DEFAULT now()
);

Alert Logic:
class MaintenanceAlert {
  async checkDueMaintenance(vehicleId) {
    const vehicle = await this.getVehicle(vehicleId);
    const currentOdo = vehicle.current_odo;
    
    const dueItems = await supabase
      .from('maintenance_schedule')
      .select('*')
      .eq('vehicle_id', vehicleId)
      .or(`next_due_odo.lte.${currentOdo},next_due_date.lte.${new Date().toISOString()}`);
    
    if (dueItems.length > 0) {
      // Send alert
      await this.sendAlert({
        type: 'MAINTENANCE_DUE',
        vehicle: vehicle.registration_no,
        items: dueItems,
        estimated_cost: dueItems.reduce((sum, item) => sum + item.estimated_cost, 0)
      });
    }
  }
}

UI Alert:
┌─────────────────────────────────────────────┐
│ 🔧 แจ้งเตือนการบำรุงรักษา                  │
├─────────────────────────────────────────────┤
│ รถ: กข-1234 กรุงเทพ                         │
│ เลขไมล์ปัจจุบัน: 125,430 km                │
│                                              │
│ ⚠️ ถึงกำหนดบำรุงรักษา:                     │
│                                              │
│ 1. เปลี่ยนน้ำมันเครื่อง                     │
│    ล่าสุด: 120,000 km (เมื่อ 30 วันที่แล้ว) │
│    ควรเปลี่ยน: ทุก 5,000 km                │
│    เกิน: 430 km                              │
│    ค่าใช้จ่ายประมาณ: ฿800                   │
│                                              │
│ 2. ตรวจเบรก                                 │
│    ล่าสุด: 100,000 km (เมื่อ 3 เดือนที่แล้ว)│
│    ควรตรวจ: ทุก 6 เดือน                     │
│    เหลือเวลา: 3 เดือน                       │
│    ค่าใช้จ่ายประมาณ: ฿1,200                 │
│                                              │
│ 💡 คำแนะนำ:                                │
│ ควรนัดหมายอู่ภายใน 2 สัปดาห์               │
│ ประมาณการค่าใช้จ่ายรวม: ฿2,000             │
│                                              │
│ [📅 นัดหมายอู่] [⏰ เตือนฉันภายหลัง]       │
└─────────────────────────────────────────────┘

Benefits:
- ลดการเสียของรถกลางทาง
- วางแผนงบประมาณซ่อมได้
- ยืดอายุการใช้งานรถ
- ลดต้นทุนซ่อมฉุกเฉิน (แพงกว่า 2-3 เท่า)
```

---

### **ADVANCED FEATURES (Week 7-10)** 🟢 เพิ่มมูลค่า

#### 8. Route Efficiency Analysis
```
Goal: วิเคราะห์ว่า route ไหนคุ้มค่าที่สุด

Display:
┌─────────────────────────────────────────────────────────────┐
│ 🗺️ วิเคราะห์เส้นทาง - กรุงเทพ → เชียงใหม่                  │
├─────────────────────────────────────────────────────────────┤
│ จำนวนเที่ยวที่ผ่านมา: 48 เที่ยว (6 เดือนล่าสุด)            │
│                                                              │
│ ┌────────────────────────────────────────────────────┐     │
│ │ 🥇 เส้นทาง A: ทางหลวง 1 (ตรง)                     │     │
│ │ ระยะทาง: 680 km  |  เวลาเฉลี่ย: 9.5 ชม.           │     │
│ │ ค่าผ่านทาง: ฿450  |  น้ำมัน: 4.2 km/L            │     │
│ │ ต้นทุนเฉลี่ย: ฿1,850  |  กำไรเฉลี่ย: ฿1,650      │     │
│ │ ใช้: 28 เที่ยว (58%)  ⭐⭐⭐⭐⭐                   │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ ┌────────────────────────────────────────────────────┐     │
│ │ 🥈 เส้นทาง B: ทางหลวง 11 (ผ่านพิษณุโลก)          │     │
│ │ ระยะทาง: 720 km  |  เวลาเฉลี่ย: 10.2 ชม.          │     │
│ │ ค่าผ่านทาง: ฿320  |  น้ำมัน: 4.0 km/L            │     │
│ │ ต้นทุนเฉลี่ย: ฿1,920  |  กำไรเฉลี่ย: ฿1,580      │     │
│ │ ใช้: 15 เที่ยว (31%)  ⭐⭐⭐⭐                     │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ ┌────────────────────────────────────────────────────┐     │
│ │ 🥉 เส้นทาง C: ทางหลวง 32 (เส้นสวย)                │     │
│ │ ระยะทาง: 695 km  |  เวลาเฉลี่ย: 11.0 ชม.          │     │
│ │ ค่าผ่านทาง: ฿180  |  น้ำมัน: 3.8 km/L            │     │
│ │ ต้นทุนเฉลี่ย: ฿2,050  |  กำไรเฉลี่ย: ฿1,450      │     │
│ │ ใช้: 5 เที่ยว (11%)  ⭐⭐⭐                        │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ 💡 คำแนะนำ:                                                │
│ - เส้นทาง A ให้กำไรดีที่สุด (ใช้บ่อยที่สุดแล้ว ✅)         │
│ - หลีกเลี่ยงเส้นทาง C (ทางโค้งมาก ใช้น้ำมันมาก)           │
│ - ถ้าเลี่ยงรถติด แนะนำเส้นทาง B                            │
│                                                              │
│ 📊 ถ้าใช้แต่เส้นทาง A ตลอด:                                │
│ ประหยัดได้: ฿9,600/ปี (vs mixed routes)                    │
└─────────────────────────────────────────────────────────────┘

SQL Query:
CREATE VIEW v_route_analysis AS
SELECT 
  CONCAT(origin_province, ' → ', destination_province) as route_name,
  route_details->>'highway' as highway,
  COUNT(*) as trip_count,
  AVG(distance_km) as avg_distance,
  AVG(EXTRACT(EPOCH FROM (delivered_at - picked_up_at))/3600) as avg_hours,
  AVG(tc.toll_total) as avg_toll,
  AVG(tc.fuel_total_cost / (distance_km / 100)) as fuel_cost_per_100km,
  AVG(tc.net_profit) as avg_profit,
  AVG(f.fuel_efficiency_kml) as avg_fuel_efficiency
FROM trips t
JOIN trip_costs tc ON tc.trip_id = t.id
JOIN fuel_records f ON f.trip_id = t.id
WHERE t.status = 'DELIVERED'
GROUP BY route_name, highway
HAVING COUNT(*) >= 5
ORDER BY avg_profit DESC;
```

---

#### 9. Income & Expense Report Generator
```
Goal: สร้าง report อัตโนมัติสำหรับภาษี/บัญชี

Features:
- Daily/Weekly/Monthly/Yearly reports
- Export to Excel/PDF
- Email auto-send
- Tax-ready format

UI:
┌─────────────────────────────────────────────────────────────┐
│ 📊 รายงานรายรับ-รายจ่าย                                     │
├─────────────────────────────────────────────────────────────┤
│ ช่วงเวลา: [มกราคม 2026 ▾]  ประเภท: [รายเดือน ▾]          │
│ รถ: [ทั้งหมด ▾]  คนขับ: [ทั้งหมด ▾]                        │
│                                                              │
│ 💰 REVENUE (รายรับ)                                         │
│ ┌────────────────────────────────────────────────────┐     │
│ │ ค่าขนส่ง:                    ฿245,000             │     │
│ │ ค่าบริการพิเศษ:              ฿12,500              │     │
│ │ อื่นๆ:                        ฿3,800               │     │
│ │ ────────────────────────────────                   │     │
│ │ รวมรายรับ:                   ฿261,300             │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ 💸 EXPENSES (รายจ่าย)                                       │
│ ┌────────────────────────────────────────────────────┐     │
│ │ น้ำมัน:                      ฿89,500  (34%)       │     │
│ │ ค่าผ่านทาง:                  ฿8,400   (3%)        │     │
│ │ ซ่อมบำรุง:                   ฿15,800  (6%)        │     │
│ │ ค่าแรงคนขับ:                 ฿45,000  (17%)       │     │
│ │ ค่าเสื่อมราคา:               ฿28,000  (11%)       │     │
│ │ ประกัน:                      ฿6,500   (2%)        │     │
│ │ อื่นๆ:                       ฿4,200   (2%)        │     │
│ │ ────────────────────────────────                   │     │
│ │ รวมรายจ่าย:                  ฿197,400             │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ 📈 NET PROFIT (กำไรสุทธิ)                                   │
│ ┌────────────────────────────────────────────────────┐     │
│ │ ฿63,900  (24% margin)                              │     │
│ │ เปรียบเทียบเดือนก่อน: +฿5,200 (+8.9%)            │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ 📊 CHARTS                                                   │
│ [Pie chart: Expense breakdown]                              │
│ [Line chart: Revenue vs Expenses trend]                     │
│                                                              │
│ [📥 Export Excel] [📄 Export PDF] [📧 Email]               │
└─────────────────────────────────────────────────────────────┘

Implementation:
class ReportGenerator {
  async generateMonthlyReport(month, year, filters) {
    // Query all data
    const revenue = await this.getRevenue(month, year, filters);
    const expenses = await this.getExpenses(month, year, filters);
    const breakdown = await this.getExpenseBreakdown(month, year, filters);
    
    // Generate Excel
    const workbook = XLSX.utils.book_new();
    const worksheet = XLSX.utils.json_to_sheet([
      { Category: 'Revenue', Amount: revenue.total },
      { Category: 'Expenses', Amount: expenses.total },
      { Category: 'Net Profit', Amount: revenue.total - expenses.total },
      {},
      { Category: 'Expense Breakdown', Amount: '' },
      ...breakdown
    ]);
    
    XLSX.utils.book_append_sheet(workbook, worksheet, 'Summary');
    
    return workbook;
  }
  
  async generatePDF(data) {
    // Use jsPDF or similar
  }
  
  async emailReport(reportData, recipients) {
    // Send via email
  }
}
```

---

#### 10. Driver Incentive System
```
Goal: Motivate drivers ด้วยระบบ bonus/reward

Incentive Types:
1. Performance-based
   - On-time delivery > 95%: +฿500/week
   - Fuel efficiency > average: +฿300/week
   - Zero accidents: +฿1,000/month
   - High customer rating: +฿200/week

2. Volume-based
   - Complete 15 trips/week: +฿1,000
   - Complete 20 trips/week: +฿2,000
   - Complete 60 trips/month: +฿5,000

3. Achievement-based
   - Best driver of the month: +฿3,000
   - Most improved driver: +฿2,000
   - Safety award (6 months no accident): +฿5,000

UI:
┌─────────────────────────────────────────────────────────────┐
│ 🏆 ระบบแต้มและรางวัล                                        │
├─────────────────────────────────────────────────────────────┤
│ คะแนนสะสม: 2,450 แต้ม  🔥 Streak: 12 วัน                  │
│                                                              │
│ 🎯 เป้าหมายสัปดาห์นี้                                       │
│ ┌────────────────────────────────────────────────────┐     │
│ │ ส่งตรงเวลา 10 เที่ยว                               │     │
│ │ ████████░░ 8/10  (ขาดอีก 2!)                       │     │
│ │ รางวัล: +500 แต้ม (+฿500)                          │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ ┌────────────────────────────────────────────────────┐     │
│ │ ประหยัดน้ำมันกว่าค่าเฉลี่ย                         │     │
│ │ ████████░░ 4.2/4.0 km/L  ✅ สำเร็จ!                │     │
│ │ รางวัล: +300 แต้ม (+฿300)                          │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ 🏅 ความสำเร็จ (Achievements)                                │
│ ┌────────────────────────────────────────────────────┐     │
│ │ ✅ ไร้อุบัติเหตุ 30 วัน      +1,000 แต้ม         │     │
│ │ ✅ Best Driver ธ.ค. 2025    +3,000 แต้ม         │     │
│ │ ⏳ ส่งตรงเวลา 100 เที่ยว      92/100             │     │
│ │ ⏳ ขับรถ 50,000 km           48,200/50,000        │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ 💰 แลกรางวัล                                               │
│ ┌────────────────────────────────────────────────────┐     │
│ │ 🎁 บัตรน้ำมัน ฿500         (500 แต้ม)  [แลก]    │     │
│ │ 🎁 เงินสด ฿1,000            (1,000 แต้ม) [แลก]    │     │
│ │ 🎁 วันหยุด 1 วัน            (2,000 แต้ม) [แลก]    │     │
│ │ 🎁 โบนัสพิเศษ ฿5,000       (5,000 แต้ม) [🔒]     │     │
│ └────────────────────────────────────────────────────┘     │
│                                                              │
│ 🏆 Leaderboard (สัปดาห์นี้)                                │
│ 1. สมชาย ใจดี       3,850 แต้ม  ⭐⭐⭐⭐⭐               │
│ 2. สมศรี รักษ์สุข    3,200 แต้ม  ⭐⭐⭐⭐⭐               │
│ 3. คุณ (สมหมาย)      2,450 แต้ม  ⭐⭐⭐⭐                 │
│ 4. สมปอง วิ่งเร็ว    2,100 แต้ม  ⭐⭐⭐⭐                 │
└─────────────────────────────────────────────────────────────┘

Database:
CREATE TABLE driver_incentives (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  driver_user_id text,
  incentive_type text,
  points_earned int,
  cash_bonus numeric,
  earned_at timestamptz DEFAULT now(),
  reason text,
  trip_id bigint REFERENCES trips(id)
);

CREATE TABLE driver_achievements (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  driver_user_id text,
  achievement_type text,
  achievement_name text,
  points_awarded int,
  unlocked_at timestamptz DEFAULT now()
);

CREATE TABLE driver_rewards_redemption (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  driver_user_id text,
  reward_name text,
  points_spent int,
  cash_value numeric,
  redeemed_at timestamptz DEFAULT now(),
  status text DEFAULT 'PENDING' -- PENDING, APPROVED, PAID
);
```

---

## 🗄️ Database Schema Summary

```sql
-- New tables needed

-- 1. Fuel records
CREATE TABLE fuel_records (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  trip_id bigint REFERENCES trips(id),
  driver_user_id text,
  vehicle_id bigint,
  fuel_amount_liters numeric,
  fuel_price_per_liter numeric,
  fuel_total_cost numeric,
  odo_reading numeric,
  fuel_efficiency_kml numeric,
  receipt_photo_url text,
  created_at timestamptz DEFAULT now()
);

-- 2. Trip costs (already defined above)
-- See section 5

-- 3. Maintenance schedule & records (already defined above)
-- See section 7

-- 4. Driver performance metrics
CREATE TABLE driver_performance_daily (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  driver_user_id text,
  date date,
  trips_completed int,
  total_distance_km numeric,
  total_duration_hours numeric,
  fuel_efficiency_kml numeric,
  on_time_deliveries int,
  late_deliveries int,
  revenue_today numeric,
  costs_today numeric,
  net_profit numeric,
  performance_score numeric,
  rank_today int,
  created_at timestamptz DEFAULT now(),
  UNIQUE(driver_user_id, date)
);

-- 5. Driver incentives (already defined above)
-- See section 10

-- 6. Route analytics
CREATE TABLE route_history (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  trip_id bigint REFERENCES trips(id),
  origin_lat numeric,
  origin_lng numeric,
  origin_province text,
  destination_lat numeric,
  destination_lng numeric,
  destination_province text,
  route_details jsonb, -- highway, waypoints, etc.
  distance_km numeric,
  duration_hours numeric,
  created_at timestamptz DEFAULT now()
);

-- Views for analytics
CREATE VIEW v_driver_monthly_stats AS
SELECT 
  driver_user_id,
  DATE_TRUNC('month', date) as month,
  SUM(trips_completed) as total_trips,
  SUM(total_distance_km) as total_distance,
  AVG(fuel_efficiency_kml) as avg_fuel_efficiency,
  SUM(on_time_deliveries)::float / NULLIF(SUM(trips_completed), 0) * 100 as on_time_rate,
  SUM(revenue_today) as total_revenue,
  SUM(costs_today) as total_costs,
  SUM(net_profit) as total_profit,
  AVG(performance_score) as avg_score
FROM driver_performance_daily
GROUP BY driver_user_id, DATE_TRUNC('month', date);

CREATE VIEW v_vehicle_efficiency_ranking AS
SELECT 
  v.id,
  v.registration_no,
  v.vehicle_type,
  AVG(f.fuel_efficiency_kml) as avg_fuel_efficiency,
  COUNT(t.id) as total_trips,
  SUM(tc.net_profit) as total_profit,
  AVG(tc.profit_margin) as avg_profit_margin,
  RANK() OVER (ORDER BY AVG(tc.profit_margin) DESC) as profit_rank
FROM vehicles v
JOIN trips t ON t.vehicle_id = v.id
JOIN fuel_records f ON f.trip_id = t.id
JOIN trip_costs tc ON tc.trip_id = t.id
WHERE t.created_at > NOW() - INTERVAL '30 days'
GROUP BY v.id, v.registration_no, v.vehicle_type;
```

---

## 📱 UI/UX Improvements

### **Navigation Enhancement**
```
Add new menu items:

Current:
- หน้าหลัก
- ค้นหางาน
- ประวัติ
- ตั้งค่า

New:
- หน้าหลัก
- ค้นหางาน
- 📊 Dashboard (NEW!)
  - ภาพรวมวันนี้
  - สถิติสัปดาห์/เดือน
  - เปรียบเทียบประสิทธิภาพ
- 💰 รายรับ-รายจ่าย (NEW!)
  - บันทึกค่าใช้จ่าย
  - รายงานรายเดือน
  - ดาวน์โหลด Excel/PDF
- 🏆 แต้มและรางวัล (NEW!)
  - คะแนนสะสม
  - ความสำเร็จ
  - แลกรางวัล
- 🔧 บำรุงรักษา (NEW!)
  - ตารางบำรุงรักษา
  - แจ้งเตือน
  - ประวัติการซ่อม
- ประวัติ
- ตั้งค่า
```

---

## 🎯 Implementation Priority

### **Week 1-2: Foundation (MUST HAVE)** 🔴
1. ✅ Fuel tracking + efficiency display
2. ✅ Basic cost entry form
3. ✅ Simple daily dashboard

### **Week 3-4: Core Value (HIGH PRIORITY)** 🟡
4. ✅ Driver performance score
5. ✅ Trip cost calculator with breakdown
6. ✅ Basic reports (daily/weekly)

### **Week 5-6: Owner Value (IMPORTANT)** 🟡
7. ✅ Vehicle performance comparison
8. ✅ Maintenance alerts
9. ✅ Monthly reports with Excel export

### **Week 7-8: Advanced Features (NICE TO HAVE)** 🟢
10. ✅ Route efficiency analysis
11. ✅ Incentive system
12. ✅ Predictive analytics

---

## 💰 Business Value

### **For Drivers:**
```
Before: รายได้ไม่แน่นอน ไม่รู้ว่าประหยัดได้หรือไม่
After:  เห็นข้อมูลชัด → ปรับปรุงได้ → เพิ่มรายได้ 10-15%

Example:
- ดูแลรถดีขึ้น → ลดค่าซ่อม ฿500/เดือน
- ขับประหยัดน้ำมัน → ลดค่าน้ำมัน ฿800/เดือน
- ทำงานมีประสิทธิภาพ → ได้ bonus ฿2,000/เดือน
Total: +฿3,300/เดือน = +฿39,600/ปี
```

### **For Truck Owners:**
```
Before: รถ 10 คัน กำไรต่ำ ไม่รู้ปัญหา
After:  เห็นข้อมูลแต่ละคัน → แก้ปัญหาจุดอ่อน → เพิ่มกำไร 20-30%

Example:
Fleet 10 คัน, กำไร ฿30,000/คัน/เดือน = ฿300,000/เดือน

Improvements:
- Optimize fuel: +5% = +฿15,000/เดือน
- Reduce maintenance: +3% = +฿9,000/เดือน
- Increase utilization: +10% = +฿30,000/เดือน
Total: +฿54,000/เดือน = +฿648,000/ปี (18% increase!)
```

---

## 🚀 Quick Start Implementation

### **This Week - Minimum Viable Features:**

**Day 1-2: Fuel Tracking**
```javascript
// Add to existing check-out flow

// When driver ends trip:
async function completeTrip(tripId) {
  // Existing code...
  
  // NEW: Prompt for fuel data
  const fuelData = await promptFuelEntry();
  
  if (fuelData) {
    await saveFuelRecord(tripId, fuelData);
    
    // Calculate efficiency
    const efficiency = calculateFuelEfficiency(
      fuelData.odo_end - fuelData.odo_start,
      fuelData.fuel_liters
    );
    
    // Show feedback
    showFuelEfficiencyFeedback(efficiency);
  }
}

function promptFuelEntry() {
  return Swal.fire({
    title: '⛽ บันทึกข้อมูลน้ำมัน',
    html: `
      <input id="fuel-liters" class="swal2-input" placeholder="จำนวนลิตร" type="number">
      <input id="fuel-price" class="swal2-input" placeholder="ราคา/ลิตร" type="number">
    `,
    showCancelButton: true,
    confirmButtonText: 'บันทึก',
    cancelButtonText: 'ข้าม'
  }).then((result) => {
    if (result.isConfirmed) {
      return {
        fuel_liters: document.getElementById('fuel-liters').value,
        fuel_price_per_liter: document.getElementById('fuel-price').value
      };
    }
    return null;
  });
}
```

**Day 3-4: Simple Dashboard**
```html
<!-- Add new dashboard.html page -->
<div class="dashboard">
  <h2>📊 ภาพรวมวันนี้</h2>
  
  <div class="stats-grid">
    <div class="stat-card">
      <div class="stat-label">เที่ยววันนี้</div>
      <div class="stat-value" id="trips-today">0</div>
    </div>
    
    <div class="stat-card">
      <div class="stat-label">ระยะทาง</div>
      <div class="stat-value" id="distance-today">0 km</div>
    </div>
    
    <div class="stat-card">
      <div class="stat-label">ประสิทธิภาพน้ำมัน</div>
      <div class="stat-value" id="fuel-efficiency">0 km/L</div>
    </div>
    
    <div class="stat-card">
      <div class="stat-label">รายได้</div>
      <div class="stat-value" id="revenue-today">฿0</div>
    </div>
  </div>
  
  <div class="performance-score">
    <h3>🏆 Driver Score</h3>
    <div class="score-circle">
      <span id="score-value">0</span>/100
    </div>
  </div>
</div>

<script>
async function loadDashboard() {
  const today = new Date().toISOString().split('T')[0];
  
  // Load today's stats
  const { data } = await supabase
    .from('driver_performance_daily')
    .select('*')
    .eq('driver_user_id', userId)
    .eq('date', today)
    .single();
  
  if (data) {
    document.getElementById('trips-today').textContent = data.trips_completed;
    document.getElementById('distance-today').textContent = data.total_distance_km + ' km';
    document.getElementById('fuel-efficiency').textContent = data.fuel_efficiency_kml.toFixed(1) + ' km/L';
    document.getElementById('revenue-today').textContent = '฿' + data.revenue_today.toLocaleString();
    document.getElementById('score-value').textContent = Math.round(data.performance_score);
  }
}
</script>
```

**Day 5: Deploy & Test**
- Deploy to GitHub Pages
- Test with 2-3 drivers
- Collect feedback
- Iterate

---

**Summary:** เพิ่มฟีเจอร์เหล่านี้จะทำให้ Driver และ Truck Owner:
1. **รู้จักตัวเอง** - เห็นข้อมูลจริง ไม่ต้องเดา
2. **ปรับปรุงได้** - มี insights เพื่อเปลี่ยนแปลง
3. **เพิ่มรายได้** - ทำงานมีประสิทธิภาพมากขึ้น
4. **ลดต้นทุน** - หาจุดสิ้นเปลืองและแก้ไข
5. **วางแผนได้** - รู้ว่าต้องซ่อมเมื่อไหร่ ต้นทุนเท่าไหร่

**ROI: Features เหล่านี้จะ pay back ภายใน 1-2 เดือน จากการลดต้นทุนและเพิ่มประสิทธิภาพ** 🚀
