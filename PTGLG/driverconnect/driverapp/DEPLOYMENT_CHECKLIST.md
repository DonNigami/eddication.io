# 🔍 Checklist: ตรวจสอบ GitHub Deployment

## ขั้นตอนตรวจสอบทีละขั้น

กรุณารันคำสั่งเหล่านี้และบอกผลให้ผมทราบครับ:

---

## ✅ Step 1: ตรวจสอบว่า Push สำเร็จ

```bash
cd D:\VS_Code_GitHub_DATA\eddication.io\eddication.io\PTGLG\driverconnect\driverapp
git remote -v
```

**ควรเห็น:**
```
origin  https://github.com/YOUR_USERNAME/driver-connect.git (fetch)
origin  https://github.com/YOUR_USERNAME/driver-connect.git (push)
```

**บอกผม:**
- GitHub username ของคุณคืออะไร?
- Repository URL คืออะไร?

---

## ✅ Step 2: ตรวจสอบว่าไฟล์ขึ้น GitHub แล้ว

เปิด browser ไปที่:
```
https://github.com/YOUR_USERNAME/driver-connect
```

**ตรวจสอบว่ามีไฟล์เหล่านี้:**
- [ ] `index-supabase.html`
- [ ] `js/edge-functions-api.js`
- [ ] `js/config.js` (ถ้ามี)

**บอกผม:**
- เห็นไฟล์ครบไหม?
- Screenshot หน้า repo ส่งมาได้ไหม?

---

## ✅ Step 3: ตรวจสอบ GitHub Pages Settings

1. ไปที่: `https://github.com/YOUR_USERNAME/driver-connect/settings/pages`

2. ตรวจสอบ:
   - [ ] **Source**: Deploy from a branch
   - [ ] **Branch**: main (หรือ master)
   - [ ] **Folder**: / (root)
   - [ ] **Status**: มีข้อความว่า "Your site is live at..."

3. **บอกผม:**
   - GitHub Pages URL คืออะไร?
   - Status เป็น "live" แล้วหรือยัง?

---

## ✅ Step 4: ทดสอบ URL

เปิด browser ไปที่:
```
https://YOUR_USERNAME.github.io/driver-connect/index-supabase.html
```

**บอกผม:**
- [ ] หน้าเว็บโหลดไหม?
- [ ] เห็นหน้า UI ไหม?
- [ ] มี error message อะไรไหม?

**ถ้าไม่โหลด:**
- กด F12 เปิด DevTools
- ไปที่ tab Console
- มี error message อะไรบ้าง? (แปะมาให้ผมดู)

---

## ✅ Step 5: ตรวจสอบ LINE Developers Console

1. เข้า: https://developers.line.biz/console/
2. หา Provider ของคุณ
3. หา LIFF app (ID: `2007705394-Fgx9wdHu`)
4. ตรวจสอบ **Endpoint URL**

**บอกผม:**
- Endpoint URL ตั้งเป็นอะไร?
- ตรงกับ GitHub Pages URL ไหม?

**ตัวอย่างที่ถูกต้อง:**
```
Endpoint URL: https://YOUR_USERNAME.github.io/driver-connect/index-supabase.html
```

---

## ✅ Step 6: ทดสอบ LIFF URL

1. ส่งข้อความนี้ให้ตัวเองใน LINE:
   ```
   https://liff.line.me/2007705394-Fgx9wdHu
   ```

2. คลิกลิงก์ (ต้องคลิกใน LINE app)

3. **บอกผม:**
   - เปิดหน้าเว็บไหม?
   - เห็นอะไร?
   - มี error message ไหม?

**ถ้ามี error:**
- Screenshot ส่งมาให้ผมดูครับ

---

## ✅ Step 7: ตรวจสอบ Console Logs

ถ้าเปิดหน้าได้แต่ LIFF ไม่ทำงาน:

1. เปิดหน้าใน Chrome/Edge บน PC (เพื่อดู Console)
2. เข้า: `https://YOUR_USERNAME.github.io/driver-connect/index-supabase.html`
3. กด F12 → Console tab

**บอกผม:**
- เห็น log `🔄 Initializing LIFF with ID: 2007705394-Fgx9wdHu` ไหม?
- เห็น log `✅ LIFF initialized` ไหม?
- มี error สีแดงไหม? (แปะมาทั้งหมด)

---

## ✅ Step 8: ตรวจสอบไฟล์ที่จำเป็น

เปิด:
```
https://YOUR_USERNAME.github.io/driver-connect/js/edge-functions-api.js
```

**บอกผม:**
- โหลดไฟล์ได้ไหม?
- เห็นโค้ด JavaScript ไหม?
- หรือได้ "404 Not Found"?

---

## 🆘 Common Issues

### ❌ Issue 1: หน้าเว็บไม่โหลด (404)
**สาเหตุ:**
- GitHub Pages ยังไม่เปิด
- ชื่อไฟล์ผิด
- Branch ผิด

**แก้:**
1. รอ 2-3 นาทีหลัง push
2. ตรวจสอบ Settings → Pages
3. ตรวจสอบว่าไฟล์ชื่อ `index-supabase.html` (ไม่ใช่ `index.html`)

### ❌ Issue 2: "Mixed Content" Error
**สาเหตุ:** มีการเรียก HTTP แทน HTTPS

**แก้:** ตรวจสอบว่าทุก resource เป็น HTTPS
```html
<!-- ✅ ถูก -->
<script src="https://static.line-scdn.net/liff/edge/2/sdk.js"></script>

<!-- ❌ ผิด -->
<script src="http://static.line-scdn.net/liff/edge/2/sdk.js"></script>
```

### ❌ Issue 3: "js/edge-functions-api.js 404"
**สาเหตุ:** ไฟล์ไม่ได้ถูก push

**แก้:**
```bash
cd D:\VS_Code_GitHub_DATA\eddication.io\eddication.io\PTGLG\driverconnect\driverapp
git add js/edge-functions-api.js
git commit -m "Add edge functions API"
git push
```

### ❌ Issue 4: LIFF "Endpoint URL mismatch"
**สาเหตุ:** URL ไม่ตรงกับที่ตั้งใน Console

**แก้:**
1. เช็ค GitHub Pages URL
2. ตั้งค่า Endpoint URL ให้ตรงกัน
3. บันทึก

### ❌ Issue 5: "🧪 กำลังใช้งานแบบทดสอบ"
**นี่ไม่ใช่ error!** 

**สาเหตุ:** เปิดใน browser ธรรมดา (ไม่ใช่ LINE app)

**แก้:** เปิดผ่าน LIFF URL ใน LINE app:
```
https://liff.line.me/2007705394-Fgx9wdHu
```

---

## 📋 กรอกข้อมูลเหล่านี้ส่งให้ผม:

```
GitHub Username: _________________
Repository URL: _________________
GitHub Pages URL: _________________
Endpoint URL (in LINE Console): _________________

หน้าเว็บโหลดไหม? (Yes/No): _____
มี error ใน Console ไหม? (Yes/No): _____
ถ้ามี error, error message คือ: _________________

เปิดผ่าน LIFF URL แล้วหรือยัง? (Yes/No): _____
เปิดใน LINE app หรือ browser? _________________
```

---

**บอกข้อมูลเหล่านี้มาให้ผมแล้วจะช่วยแก้ให้ทันทีครับ! 🚀**
